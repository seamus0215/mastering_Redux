import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
      This app uses React, Redux, React Router, and many more helpful libraries.
    </p>
  </div>
);

export default AboutPage;
